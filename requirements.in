# Runtime dependencies — source of truth.
# DO NOT edit requirements.txt directly; edit this file and run:
#   pip-compile --generate-hashes requirements.in
#
# To upgrade all packages:
#   pip-compile --upgrade --generate-hashes requirements.in

# FastAPI pinned to <0.111: 0.111+ requires pydantic>=2.7 AND starlette>=0.40.0.
# starlette>=0.40.0 requires Python>=3.10 — blocked until SQ-DEP-05.
# Pydantic v2 migration affects 20 models (class Config) + 17 (schema_extra) + 6 (.dict()) + 1 (BaseSettings).
# Upgrade path: SQ-DEP-05 (Python 3.11) → SQ-DEP-02 full migration.
fastapi>=0.110.0,<0.111.0
uvicorn[standard]>=0.29.0,<1.0
python-multipart>=0.0.18,<0.0.21  # Max compatible with Python 3.9; CVEs that require 0.0.22+ need Py3.10 (SQ-DEP-05)
pydantic>=1.10.0,<2.0  # v2 migration: schema_extra->json_schema_extra, .dict()->.model_dump(), BaseSettings->pydantic-settings

python-dotenv>=1.2.0,<2.0

# Azure AI Document Intelligence
# Upgraded from 1.0.0b4 (beta) to 1.0.2 (GA stable) in SQ-DEP-04.
# API is backward-compatible: DocumentIntelligenceClient and AnalyzeOutputOption unchanged.
azure-ai-documentintelligence>=1.0.2,<2.0
azure-core>=1.35.0,<2.0
azure-identity>=1.22.0,<2.0

# PDF / image processing
PyMuPDF>=1.23.25,<2.0
Pillow>=10.3.0,<12.0  # 12.x requires Python 3.10; upgrade blocked until SQ-DEP-05

# HTTP client
httpx>=0.28.0,<1.0

# MongoDB
motor>=3.3.0,<4.0
pymongo>=4.6.3,<5.0
